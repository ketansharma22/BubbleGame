function generateBubble(){
    var lower=document.querySelector('#lower').innerHTML;
    for(var i=1;i<100;i++){
        let val=Math.floor(mat)
        var bubble=`<div class="bubble">${val}</div>`
    }
}